// PlayKit Library
include ':playkit'
project(':playkit').projectDir = new File('../../playkit-android/playkit')

// Sample apps
def samples = [
        'BasicSetup', 
        'EventsRegistration', 
        'BasicPluginsSetup', 
        'TracksSelection', 
        'ChangeMedia',
        'AnalyticsOTT', 
        'IMASample', 
        'Offline', 
        'Youbora',
]

samples.each {
    def entry = ':' + it
    include entry
    project(entry).projectDir = new File ('../' + it + '/app')
} 
    
